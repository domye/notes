[[toc]]

## Redis有哪些内存淘汰策略？  

默认的是不删除任何key，直接返回错误，生产环境一般不使用

然后是针对所有 key 的 allkeys-lru、 allkeys-lfu 和 allkeys-random ，lru删除最近少使用的key。lfu会删除访问频率最低的key，random随即删除

其次是针对设置了过期时间的 key，有 volatile-lru、 volatile-lfu、 volatile-ttl 和 volatile-random  